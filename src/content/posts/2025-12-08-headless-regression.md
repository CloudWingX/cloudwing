---
title: 用无头浏览器给自己写回归测试
summary: 几何断言优于肉眼：量矩形、比重叠、数像素——站点改版不再靠"看着没问题"。
date: 2025-12-08
tags: [测试, CDP, 前端]
---

静态站改版最怕"改了一处、坏了三处"。截图肉眼看不可靠，我给站点配了一套基于 Chrome DevTools Protocol 的验证脚本，核心思路是把每条设计规格变成**可执行的断言**：

- **几何断言优于肉眼**：直接量元素的矩形，比较左右缘是否重合、两栏是否等高、有没有横向溢出（`scrollWidth - clientWidth`）；
- **对比度算真实合成色**：渐变文字取所有停靠点里最差的那个，半透明文字先按 alpha 合成到底色再算；
- **写新断言必须先证明"它在缺陷态下会红"**——把出问题的样式注回去重跑，确认变红再删掉注入。否则就是一条恒真断言，绿灯给人假安心。

这套东西最大的收益不是省时间，而是**改版时的心理安全感**：每条断言都对应一条当初踩过的坑，绿灯亮起，等于所有旧坑都重新检查了一遍。
