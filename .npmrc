# npm 配置（随仓库提交，供 Cloudflare 构建使用）
# 1) 无 lock 文件环境（本项目有意不提交 package-lock.json）：忽略 peer 冲突，
#    避免上游依赖小版本升级导致 ERESOLVE 构建失败；
# 2) react/react-dom 已在 package.json 固定为精确版本 19.2.8，
#    以匹配 @react-three/fiber 的 peer 范围（>=19 <19.3）。
legacy-peer-deps=true
audit=false
fund=false
