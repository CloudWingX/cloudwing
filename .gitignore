# build output
dist/
# generated types
.astro/
# dependencies
node_modules/
# package-lock: Cloudflare Pages 检测到 lock 会用 npm ci，会因平台差异失败；
# 有意不提交 —— 让云端用 npm install 按 Linux 动态解析
package-lock.json
# logs
npm-debug.log*
# environment
.env
.env.production

# 内部交接文档与本地参考文件（不进公开仓库）
SESSION_HANDOFF.md
.ref-*

# editor / os
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
